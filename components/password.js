import { withStyles } from "material-ui/styles";
import TextField from "material-ui/TextField";

const styles = {};

const Password = ({ classes }) => <TextField label="password" />;

export default withStyles(styles)(Password);
